The files tictactoeaz.py and tictactoebackendaz.py are both located in the alpha-zero-general directory and should be run
from there. They are copied into this directory for safe keeping because the alpha-zero-general directory is not my own
repository. 